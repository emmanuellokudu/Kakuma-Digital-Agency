window.KDA_SITE_CONFIG = Object.freeze({
  // Add digits only, including country code. Example format: "254700000000".
  // This is intentionally blank until KDA provides and verifies the number.
  whatsappNumber: "254702900551",
  // Public browser credentials from https://dashboard.emailjs.com/.
  emailjs: Object.freeze({
    publicKey: "bOmflMI23IpdBFIQT",
    serviceId: "service_kk0iux7",
    templateId: "template_hgadsqy",
  }),
});
